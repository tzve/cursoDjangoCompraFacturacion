server {
    listen 80;

    # Ruta para servir archivos estáticos (CSS, JS, imágenes)
    location /static/ {
        alias /app/staticfiles/;   # la misma ruta que STATIC_ROOT en el contenedor
        access_log off;
        expires 1y;
    }

    # Ruta para servir archivos media (imágenes subidas)
    location /media/ {
        alias /app/media/;
        access_log off;
        expires 30d;
        add_header Cache-Control "public";
    }

    # Redirige todas las demás peticiones al contenedor de Django (Gunicorn)
    location / {
        proxy_pass http://web:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
